// --- Chrollo — Retrieval layer over Pi's native sessions (0.3.0 rewrite) ---
// Phase 0 stub. The search + read tools and the session_start cwd capture land
// in Phase 6. See .vscode/SPEC.md for the architecture pivot rationale.

import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function chrolloExtension(_pi: ExtensionAPI): void {
  // Intentionally empty during the rewrite.
}
